// This is a placeholder test file for firstTimeUserGuide.tsx
// Since we don't have testing dependencies installed, we're just verifying the file can be imported

import { showFirstTimeUserWelcome, showFirstGuruCreatedMessage, FirstTimeUserFormGuide, FirstTimeUserWelcomeModal } from "../firstTimeUserGuide";

// Verify that the imports work without errors
console.log("FirstTimeUserGuide imports successfully");

// Export for potential future use
export { showFirstTimeUserWelcome, showFirstGuruCreatedMessage, FirstTimeUserFormGuide, FirstTimeUserWelcomeModal };);